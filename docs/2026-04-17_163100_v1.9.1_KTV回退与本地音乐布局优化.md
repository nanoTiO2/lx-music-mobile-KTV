# v1.9.1 KTV回退与本地音乐布局优化

时间：2026-04-17 16:31:00（东八区）

## 本次调整

1. KTV 切换回退到上个稳定调参
- `src/core/ktv/index.ts`
  - `fadeDurationMs` 从 `320` 回退到 `180`
- `android/app/src/main/java/cn/toside/music/mobile/mixer/MixerModule.java`
  - `STANDBY_PREROLL_MS` 从 `96` 回退到 `48`
- 目的：恢复到用户确认“可切换且不会残留音轨”的前一版参数

2. 歌词舞台页新增更多自定义
- `src/screens/LyricStage/index.tsx`
- 新增可直接在页面内切换的能力：
  - 颜色主题：翠绿 / 琥珀 / 冰蓝 / 玫红 / 黑白
  - 字体族：默认 / 黑体 / 衬线 / 等宽 / 窄体
  - 行距调节：行距- / 行距+
- 保留已有：
  - 满屏 / 三行 / 提词器
  - 左右镜像 / 上下镜像
  - 字号放大缩小
  - 投影模式

3. 本地音乐页面目录区压缩
- `src/screens/Home/Views/LocalMusic/index.tsx`
- 优化点：
  - 文件夹区改为可折叠
  - 默认优先给歌曲列表留空间
  - 根目录列表与分组列表最大高度下调
  - 行高、按钮、文字密度压缩

## 验证

- Android Debug 构建成功
- APK 已重新安装到设备 `f158b99c`

## 建议回归测试

1. KTV 播放中切换音轨，确认：
- 不再出现切换后双声部残留
- 不再出现切换后软件关闭

2. 歌词舞台页确认：
- 颜色主题切换生效
- 字体切换生效
- 行距调整生效

3. 本地音乐页确认：
- 默认目录区占用明显缩小
- 展开/收起目录正常
- 切换目录仍可正常导入和删除
