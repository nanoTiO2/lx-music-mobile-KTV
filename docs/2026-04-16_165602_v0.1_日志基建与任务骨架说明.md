# v0.1 日志基建与任务骨架说明

## 本次交付

- 新增 SQLite 日志脚本 `scripts/codex_log_db.py`。
- 新增统一入口 `main.py` 与 Windows 启动脚本 `run.bat`。
- 新增 `tests/`、`docs/`、`logs/` 目录骨架。
- 新增首份架构设计文档与本阶段说明文档。

## 已满足的规范

- SQLite 日志库固定命名为 `codex_lx二开_log.db`。
- 表结构覆盖 `timestamp`、`task_id / session_id`、`user_requirement`、`thinking_path`、`key_steps`、`code_snippet`、`error_message`、`fix_record`、`status`。
- 可记录文档、脚本等交付物路径。
- `run.bat` 先尝试 UTF-8，失败后回退到 GBK。

## 本阶段未完成

- App 内部业务日志尚未接入 SQLite。
- 下载保存目录、任务页与路径记忆尚未开始接线。
- 本地媒体聚合与切源播放仍处于设计完成、实现未开始状态。

## 下一步

- 扩展设置项：`download.saveDir`、`download.useCustomDir`。
- 接入目录选择组件与保存任务。
- 让下载页从占位页变成真实任务列表。
