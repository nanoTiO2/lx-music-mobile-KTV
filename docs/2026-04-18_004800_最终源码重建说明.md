# 最终源码重建说明

## 问题确认

2026-04-17 晚间用于规避中文路径构建问题的短路径工位 `F:\lxm` 不是当前最终源码快照。

核对结果：

- `src/screens/Home/Views/LocalMusic/index.tsx` 在当前源码中存在，但旧工位缺失。
- `src/core/ktv/index.ts`
- `src/plugins/player/utils.ts`
- `src/store/list/state.ts`
- `src/components/common/ChoosePath/index.tsx`

以上关键文件在旧工位与当前源码内容不一致，因此基于旧工位构建的 APK 会出现“主要功能缺失”。

## 处理方式

本次已放弃复用 `F:\lxm`，改为从当前源码重新同步到新的 ASCII 短路径工位：

- `F:\lxmfinal`

同步后再次核对关键功能文件，确认与当前源码一致后，重新执行：

- `assembleDebug`
- `assembleRelease`

## 结论

本次新产物才是基于当前最终源码构建的 APK。

建议后续仅使用以下两类来源：

- 当前源码工作区 `publish/2026-04-18_apk`
- 短路径最终工位 `F:\lxmfinal\android\app\build\outputs\apk`

不再使用旧工位 `F:\lxm` 的产物，避免再次混入缺功能旧包。
